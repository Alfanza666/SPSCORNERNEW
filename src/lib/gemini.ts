/**
 * Gemini AI Client — SERVER-SIDE ONLY
 *
 * PENTING: File ini TIDAK digunakan oleh komponen React/frontend.
 * Gemini AI hanya diinisialisasi di server.ts (Node.js environment).
 *
 * File ini ada hanya sebagai placeholder agar tidak ada import error
 * jika ada komponen yang tidak sengaja mengimpornya.
 *
 * Untuk keperluan server: import langsung di server.ts dengan:
 *   import { GoogleGenAI } from '@google/genai';
 */
export const ai = null;
