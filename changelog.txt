v5.9.2 — Pembersihan Komponen Accordion Formulir Pendaftaran Legacy & Perbaikan Filter Program (PATCH)
------------------------------------------------------------------------
### Perbaikan:
- **[CLEANUP] src/pages/dashboard/admin/AdminUnionPrograms.tsx:**
  Menghapus secara permanen accordion "Formulir Pendaftaran" legacy (Google Form style) dari modal buat/edit program kerja. Pengaturan formulir kini terpusat dan didelegasikan sepenuhnya ke Form Builder baru.

- **[FIX] src/pages/dashboard/admin/AdminFormBuilder.tsx:**
  Menghapus filter `.eq('is_active', true)` pada method `fetchPrograms` sehingga program yang baru dibuat (meski berstatus draft/non-aktif) tetap muncul di dropdown Program Acara pada tab Setup/Pengaturan Form Builder.


v5.9.1 — Perbaikan Alur Form Builder & Cleanup Form Program Kerja (PATCH)
------------------------------------------------------------------------
### Perbaikan:
- **[FIX] src/components/forms/PremiumFormExperience.tsx:**
  Memperbaiki bug di mana total biaya tambahan di halaman pembayaran manual selalu tampil sebagai **Rp 0**. 
  Root cause: kode UI memanggil properti `evaluation.total` padahal data evaluasi alur menggunakan nama `evaluation.total_amount`.

- **[FIX] src/pages/dashboard/admin/AdminFormBuilder.tsx:**
  Validasi "Formulir berbayar wajib dihubungkan ke Program Kerja" diubah dari
  hard block (return) menjadi soft warning toast kuning 5 detik. Form tetap
  berhasil tersimpan/diterbitkan meskipun belum tertaut ke program. Tab
  Pengaturan dibuka otomatis agar user bisa menghubungkan kapan saja.
  Validasi rekening/QRIS juga dikecualikan untuk form non-experience_version 2.
  Root cause: template RSVP mengandung harga field sehingga selalu ditolak.

- **[FIX/CLEANUP] src/pages/dashboard/admin/AdminUnionPrograms.tsx:**
  Membersihkan form pembuatan/edit Program Kerja dari field yang sudah tidak
  relevan karena fungsinya telah diambil alih oleh Form Builder RSVP.
  DIHAPUS: Toggle "Formulir Tambahan Acara" + dropdown dynamic_form_id,
  section "Gate & Tiket" (enable_meal, enable_doorprize), section "Keluarga
  Berbayar" (enable_family, family_package_price), field Tambahan XXL/XXXL
  surcharge, checkbox "Aktifkan Form/Poll".
  DITAMBAH: Card "Formulir RSVP" yang menampilkan nama form terhubung (hijau)
  atau tombol "Buat Form RSVP di Form Builder" (dashed) sebagai navigasi
  langsung ke Form Builder. Konfigurasi Gathering disederhanakan menjadi
  hanya RSVP Deadline + Kuota Maks + info box tentang Form Builder.
  Juga menambahkan import useNavigate dari react-router-dom.

### Bug JSX:
- **[FIX] AdminUnionPrograms.tsx:**
  Memperbaiki 4 error TypeScript (TS17008, TS1381, TS17002, TS1005) akibat
  tag JSX yang tidak balance: </div> penutup header Langkah 3 yang hilang
  dan satu </div> orphan yang terselip dari proses multi-replace.

v5.9.0 — Employee Gathering Workflow V3 (MINOR)

------------------------------------------------------------------------
### Root Cause Analysis:
- Sistem v5.8.x memiliki formulir dan registrasi kerja, tetapi tidak memiliki alur kerja gathering yang lengkap: publikasi transaksional, eligibilitas frozen, scan gate-aware, doorprize dari kehadiran aktual, dan pelaporan gabungan.
- Scanner dan doorprize masih menggunakan RPC legacy `claim_program_coupon` yang tidak memiliki audit trail lengkap.
- Tidak ada migration untuk program_eligibility, scan ledger V2, atau aggregate reporting view.

### Perbaikan:
- **[SECURITY]** Menghapus hardcoded fallback email Sariroti dari email.js dan misc.ts (menggunakan env var atau settings DB).
- **[FIX]** Menambahkan `IPAYMU_SIGNATURE_KEY` ke route param payments.ts (sebelumnya hilang dari server.ts).
- **[FIX]** Menambahkan `.order()` ke analytics.ts top products query (sebelumnya non-deterministic).
- **[FIX]** Mengkonsolidasi health check di lib/api.ts (menghilangkan duplikasi `isPrimaryHealthy` + `checkPrimary`).
- **[FIX]** Menyamakan routing home di KioskLayout.tsx (`getBerandaPath` dan `handleHomeClick` sekarang konsisten).
- **[FIX]** Mengkonsolidasi vendor chunk recharts di vite.config.ts (menghilangkan duplikasi `vendor-recharts` + `vendor-charts`).
- **[DATABASE]** Menambahkan migration `007_event_workflow_v3.sql`: program_type, rsvp_deadline, family_package_price, shirt_price_map, config_version, program_eligibility, scan ledger V2, doorprize_eligible, reporting aggregate view, helper functions (validate_gathering_publish, publish_gathering, scan_entitlement_v2, attendance_override).
- **[BACKEND]** Membuat `src/services/eventWorkflow.ts`: modular service untuk eligibilitas, publikasi, registrasi, pricing, entitlement, scanner, doorprize, dan pelaporan.
- **[BACKEND]** Membuat `src/routes/eventWorkflow.ts`: 15+ endpoint V2 untuk admin dan portal (preview, publish, scan, override, doorprize, report, Excel/PDF export, registration CRUD, payment review).
- **[BACKEND]** Mendaftarkan route baru di server.ts.
- **[DEPENDENCY]** Menambahkan `pdfkit` untuk export PDF server-side.
- **[FRONTEND]** Menghubungkan `AdminScanner.tsx` ke V2 scan endpoint (`POST /api/admin/program-entitlements/scan`) dengan fallback legacy.
- **[FRONTEND]** Menghubungkan `AdminUnionPrograms.tsx` ke V2 publish endpoint (`POST /api/admin/programs/:id/publish-v2`) dengan tombol "Publish V2" pada gathering programs.
- **[FRONTEND]** Menghubungkan `PortalProgram.tsx` ke V2 registration status endpoint (`GET /api/portal/programs/:id/registration-v2`) dengan fallback legacy.
- **[FRONTEND]** PORTAL-002: Tabbed UI (Kehadiran/Makan) pada `PortalProgram.tsx` dengan per-beneficiary lifecycle cards (employee/family badge, QR, status, coupon code).
- **[FRONTEND]** ADMIN-005: Tab "Laporan Program" pada `AdminReports.tsx` dengan program selector, summary cards, RSVP/payment breakdown, config info, dan Excel/PDF export.
- **[CLEANUP]** Menghapus 8 unused imports (`PartyPopper`, `Edit2`, `Eye`, `RotateCcw`, `ExternalLink`, `Clock`, `RefreshCw`, `QrCodeIcon`) dan 1 dead state (`programEligibleNiks` + `fetchProgramEligibility`).
- **[DEPENDENCY]** Menghapus 6 unused runtime dependencies: `better-sqlite3`, `cheerio`, `pdf-parse`, `react-webcam`, `@tabler/icons-react`, `remixicon-react`.
- **[VERSION]** package.json, package-lock.json, Home, DashboardLayout, PortalLayout, changelog, dan error history disinkronkan ke v5.9.0.

v5.8.2 — Form Studio V2 Review & Agent Handoff Documentation (PATCH)
------------------------------------------------------------------------
### Root Cause Analysis:
- Perubahan Form Studio, AI, RSVP, pembayaran, dan QR tersebar pada puluhan file sehingga agent baru berisiko mengulang audit atau menganggap bagian parsial sudah production-ready.
- Dokumentasi schema sebelumnya terlalu umum ketika menjelaskan write boundary; admin builder memang menulis workflow config melalui RLS, sementara transaksi responden tetap wajib melalui backend.

### Perbaikan:
- **[DOCS]** Menambahkan dokumen handoff rinci berisi workflow lama vs baru, arsitektur, alur AI, payment, QR, data model, file inventory, status implementasi, known gaps, serta urutan review.
- **[DOCS]** Menambahkan tautan dokumen handoff pada README agar agent berikutnya menemukannya saat orientasi awal.
- **[DOCS]** Menandai migration production yang belum aktif dan membedakan status implemented, partial, blocked, serta schema-only.
- **[DOCS]** Mengoreksi batas kepercayaan: admin dapat menulis config melalui RLS; respondent tidak dapat menulis tabel transaksi workflow.
- **[VERSION]** package.json, package-lock.json, Home, DashboardLayout, PortalLayout, changelog, dan error history disinkronkan ke v5.8.2.

v5.8.1 — Server-side Form Path & Answer Validation Hardening (PATCH)
------------------------------------------------------------------------
### Root Cause Analysis:
- Validasi V2 di browser sudah membatasi repeater dan opsi, tetapi endpoint registrasi belum menegakkan aturan yang sama sehingga client yang dimodifikasi dapat mengirim opsi, angka, atau data anggota keluarga yang tidak valid.
- Field setelah pilihan terminal masih dapat ikut masuk ke snapshot server apabila dikirim langsung, walaupun alur UI sudah berakhir.

### Perbaikan:
- **[SECURITY]** Server sekarang memvalidasi keanggotaan opsi, rentang angka, tipe repeater, jumlah minimum/maksimum, object setiap baris, dan subfield wajib.
- **[SECURITY]** Metode pembayaran hanya dapat dipilih dari metode yang diaktifkan admin dan keputusan upload memakai total authoritative dari server.
- **[FIX]** Pilihan terminal seperti “Tidak Hadir” memotong active path; jawaban lanjutan tidak divalidasi, dihitung, atau disimpan.
- **[FIX]** Instruksi transfer kini menampilkan nama bank yang benar, bukan kode metode pembayaran.
- **[TEST]** Menambahkan regresi untuk terminal outcome, spoofed option, number range, repeater limit, dan nama anggota wajib.
- **[VERSION]** package.json, package-lock.json, Home, DashboardLayout, PortalLayout, changelog, dan error history disinkronkan ke v5.8.1.

v5.8.0 — Premium Form Studio, Event RSVP Workflow & Manual Payment Approval (MINOR)
----------------------------------------------------------------------------------
### Root Cause Analysis:
- Builder lama masih mengedit field secara inline di canvas, sehingga ruang kerja sempit dan tampak seperti form biasa meskipun palet sudah ditambahkan.
- Preview builder dan portal pengisi memakai renderer berbeda; schema yang terlihat benar di admin dapat kosong atau berperilaku lain saat diisi.
- Submit program gathering masih berupa `setTimeout`/toast simulasi, sedangkan jalur keluarga lama mempercayai total dari browser dan dapat mengaktifkan kupon sebelum pembayaran disetujui.
- Tidak ada aggregate registrasi yang idempoten untuk mengikat jawaban, rincian harga server-side, bukti pembayaran, penerima manfaat, dan QR per peserta.

### Fitur & Perbaikan:
- **[UI/UX] Premium Form Studio:** hero workspace baru, topbar publish, device switcher, searchable/drag field palette, canvas WYSIWYG, inspector terpisah, dan logic flow map responsif.
- **[UI/UX] Premium respondent experience:** cover acara, satu pertanyaan per langkah, choice cards, progress, family repeater, review final, rincian biaya, pembayaran manual, serta outcome screen yang konsisten antara preview dan portal.
- **[FEAT] RSVP event template:** attendance → shirt size → camping → family members → configurable pricing → transfer/QRIS proof; seluruh harga, batas keluarga, rekening, QRIS, dan outcome dapat diedit dari builder.
- **[FEAT] AI schema execution V2:** AI mendukung repeater, terminal outcome, pricing, manual payment, program automation, dan selalu menerapkan schema lengkap ke canvas.
- **[BACKEND] Program Workflow V2:** server menghitung ulang harga, membersihkan hidden answers, menyimpan registrasi idempoten, menahan entitlement saat pembayaran, dan menerbitkan QR terpisah untuk attendance/meal setiap karyawan/anggota keluarga.
- **[ADMIN] Payment control center:** antrean bukti manual, approve/reject, alasan audit, penerbitan QR setelah approve, serta unlock aman untuk registrasi non-paid yang belum memiliki QR terpakai.
- **[SECURITY]** Browser tidak dapat menentukan total, user/NIK, status pembayaran, atau penerbitan kupon; semua berasal dari profil dan konfigurasi server.
- **[COMPAT]** Form lama tetap memakai renderer V1; program yang memiliki `dynamic_form_id` V2 diarahkan ke workflow baru tanpa menghapus jalur legacy yang tidak terkait.
- **[DB]** Menambahkan migration `006_program_registration_workflow_v2.sql` dan dokumentasi schema/rollout.
- **[TEST]** Menambahkan regression tests untuk terminal branches, repeater pricing, server quote, deterministic IDs, AI schema, dan workflow configuration.
- **[VERSION]** package.json, package-lock.json, Home, DashboardLayout, PortalLayout, changelog, error history, dan README disinkronkan ke v5.8.0.

v5.7.0 — Jotform Builder UX, AI Schema Execution & Conditional Form Stability (MINOR)
----------------------------------------------------------------------------------
### Root Cause Analysis:
- AI dapat mengembalikan chat sukses tanpa `updatedForm`, sehingga canvas tidak berubah.
- ID form/field dari model belum dipetakan secara aman; ID database dapat tertimpa dan conditional branch dapat terlepas setelah sanitasi.
- Halaman pengisi dan preview memakai evaluasi conditional berbeda; percabangan bertingkat, hidden required field, dan total pembayaran dapat memakai state cabang lama.
- Palette dan panel editor menggunakan layout desktop tetap sehingga editor terhimpit atau tidak dapat digunakan pada mobile/tablet.

### Fitur & Perbaikan:
- **[FEAT] Jotform-style responsive builder:** palette searchable, drag-to-canvas, mobile element drawer, mobile preview, WYSIWYG field cards, dan side panel overlay adaptif.
- **[FEAT] Field coverage:** palette/editor mendukung image, image choice, addon group, dan payment section dengan default konfigurasi yang valid.
- **[FIX] AI direct execution:** respons message-only diperbaiki otomatis sekali; tanpa schema endpoint gagal eksplisit, sedangkan schema valid langsung diterapkan ke canvas.
- **[SECURITY] AI endpoint:** token dan role admin diverifikasi, rate limit ditambahkan, serta konfigurasi `GROQ_API_KEY` divalidasi tanpa fallback kredensial untuk request nyata.
- **[FIX] Stable identity:** ID form dari AI diabaikan; ID field dinormalisasi dan referensi conditional ikut dipetakan.
- **[FIX] Conditional engine:** ancestor-aware, kompatibel label/value lama, case-insensitive, dan jawaban whitespace dianggap kosong.
- **[FIX] Portal rendering/payment:** hidden required field tidak lagi mounted, hidden priced field tidak dihitung, dan card navigation tidak dapat menunjuk card kosong.
- **[FIX] Interactive preview:** upload file/gambar dan addon group dapat dijawab; scale/rating responsif; progress card menggunakan progress bar.
- **[TEST]** Ditambahkan regression tests untuk AI response parser, ID mapping, conditional bertingkat, label/value, dan whitespace answer.
- **[VERSION]** package.json, package-lock.json, Home, DashboardLayout, dan PortalLayout disinkronkan ke v5.7.0.

v5.6.0 — JotForm-Style Form Builder: 3-Panel Layout, AI Copilot & Tech Minimalist (MINOR)
----------------------------------------------------------------------------------
### Fitur Baru:

**🧩 AI Response Parser Engine (`src/utils/aiResponseParser.ts`):**
- **[FEAT] Smart JSON Extraction:** Parser otomatis memisahkan teks natural dari blok JSON (```json, { }, ---GENERATE---) di respons AI. Chat hanya menampilkan teks ramah, JSON disimpan ke formState secara real-time.
- **[FEAT] Sanitasi Field:** Sanitasi otomatis untuk semua tipe field — ID unik, validasi options, price, conditional logic, payment section, addon group.
- **[FEAT] Fallback Robust:** Jika AI gagal mengikuti format `updatedForm`, parser tetap bisa mengekstrak JSON dari dalam tepesan natural.

**🖼️ WYSIWYG FormCanvas (`src/components/forms/FormCanvas.tsx`):**
- **[FEAT] Live Preview Canvas:** Menggantikan pratinjau inline — rendering form langsung dari formState dengan real-time update saat AI menghasilkan field baru.
- **[FEAT] Card Form Layout:** Satu pertanyaan per slide dengan welcome slide, progress indicator, dan tombol Next/Prev. Animasi transisi halus.
- **[FEAT] Classic Layout:** Semua field dalam satu halaman scroll vertikal. Mendukung banner, title, dan submit button.
- **[FEAT] Skeleton Loading:** Animasi pulse shimmer saat AI sedang men-generate form — user melihat indikasi visual bahwa form sedang disusun.

**🎨 FormFieldRenderer Component (`src/components/forms/FormFieldRenderer.tsx`):**
- **[FEAT] 15 Field Types:** Render semua tipe field (text, textarea, number, select, radio, checkbox, rating, scale, date, file_upload, image, image_choice, addon_group, payment_section) dengan gaya konsisten mengikuti theme_color, input_style, dan font_family.
- **[FEAT] Premium Card Option:** Radio/checkbox options menggunakan card-style dengan border, hover effects, dan label harga.

**↕️ Drag-and-Drop Field Reorder:**
- **[FEAT] HTML5 DnD:** Geser-urutkan field card di editor mode drag-and-drop native. Visual feedback ring highlight saat drag over.
- **[FEAT] Auto-activate:** Field yang dipindahkan otomatis teraktivasi (active state) untuk memudahkan edit lanjutan.

**📝 System Prompt Reinforcement (`src/routes/misc.ts`):**
- **[FEAT] Strict JSON Separation:** Prompt baru mewajibkan AI untuk TIDAK PERNAH menaruh JSON di key "message". Key "message" HANYA teks natural.
- **[FEAT] Ketat Format:** Larangan markdown, kode block, atau data teknis di chat message. Semua data form wajib di key "updatedForm".

### Perubahan File:
- **[NEW] src/utils/aiResponseParser.ts:** Parser AI response → chat text + formState
- **[NEW] src/components/forms/FormCanvas.tsx:** WYSIWYG form canvas (card/classic layout)
- **[NEW] src/components/forms/FormFieldRenderer.tsx:** Renderer 15 field types
- **[NEW] src/components/forms/FormSkeleton.tsx:** Loading skeleton shimmer
- **[CHANGE] src/pages/dashboard/admin/AdminFormBuilder.tsx:** Complete JotForm-style rewrite — 3-panel layout (field palette, canvas, AI copilot), FormCanvas integration, drag-and-drop, AI JSON parser, tech-minimalist styling
- **[CHANGE] src/routes/misc.ts:** System prompt diperkuat — pisah teks natural dari JSON, larangan JSON di message
- **[CHANGE] src/components/forms/FormCanvas.tsx:** Classic field card styling untuk tampilan JotForm premium
- **[CHANGE] src/components/forms/FormFieldRenderer.tsx:** Tech-minimalist restyle — clean borders, hover effects, consistent spacing
- **[DELETE] ~150 baris dead code:** renderPreviewField, getPreviewInputStyle, previewCardIndex/previewAnswers, unused imports
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx, PortalLayout.tsx:** v5.5.2 → v5.6.0 (MINOR: JotForm-style form builder)

v5.5.1 — Fix Admin Scanner Sidebar Route Link (PATCH)
----------------------------------------------------------------------------------
### Perbaikan:
- **[FIX] src/pages/dashboard/DashboardLayout.tsx:** Ganti route sidebar untuk link "Scan QR" dari `/dashboard/scanner` (yang menghasilkan 404/redirect ke home) → `/dashboard/admin/scanner` agar sesuai dengan routing aslinya di `App.tsx`.
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx, PortalLayout.tsx:** Versi dinaikkan ke v5.5.1.

v5.5.0 — Premium AI Form Builder & JotForm Visual Customization Engine (MINOR)
----------------------------------------------------------------------------------
### Fitur Baru:
- **[FEAT] JotForm-Style Card Form Layout:** Mendukung layout bertipe "Card Form" (satu pertanyaan per slide dengan animasi transisi geser Framer Motion yang halus, halaman selamat datang, progress bar, dan validasi dinamis per kartu sebelum lanjut).
- **[FEAT] Visual Customization Engine:** Fitur pengaturan gaya visual lengkap di form builder (Font Family dinamis, Input Style: rounded/sharp/underline, Background Image URL, dan transparansi Glassmorphism blur premium).
- **[FEAT] Split Screen AI Assistant Sidebar:** Menggantikan overlay AI chat full-screen lama dengan asisten AI sidebar interaktif terintegrasi langsung di editor form builder untuk mempermudah perancangan form langsung.
- **[FEAT] dynamic font loading:** Pemuatan font kustom dinamis dari Google Fonts API secara client-side di halaman pengisian formulir.

### Perbaikan:
- **[FIX] src/pages/portal/PortalFormView.tsx:** Penerapan style kustom dinamis (accent colors, backdrop-blur, custom fonts, rounded/sharp/underline input styles) ke seluruh field form input & tombol submit.
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx, PortalLayout.tsx:** Versi dinaikkan ke v5.5.0.

v5.4.1 — Excel Employee Import Diagnostics & Date Parsing Fix (PATCH)
----------------------------------------------------------------------------------
### Root Cause Analysis (RCA):
- **[RCA] Silent Import Skips:** Any db/upsert error (e.g. database constraint or invalid data format) during Excel employee import was silently caught and skipped, making it show "0 ditambahkan, 178 dilewati" without showing why.
- **[RCA] Excel/Postgres Date Mismatch:** Date values parsed from Excel could be Excel serial numbers (like `45123`) or Indonesian formatted date strings (`DD/MM/YYYY`, `DD-MM-YYYY`), which are incompatible with Postgres `date` data type, throwing a silent db insertion error.

### Perbaikan:
- **[FIX] src/pages/dashboard/admin/AdminEmployees.tsx (Import):**
  - Ditambahkan parsing tanggal excel secara kokoh (mendukung angka serial Excel, format DD/MM/YYYY, dan DD-MM-YYYY) ke format ISO YYYY-MM-DD.
  - Ditambahkan tracking detail error/baris bermasalah lengkap dengan alasan skip (misalnya: NIK/NAMA kosong, error constraint DB).
  - Ditambahkan visual modal/popup laporan hasil import yang informatif untuk menampilkan total data berhasil vs dilewati lengkap dengan rincian baris error.
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx, PortalLayout.tsx:** v5.4.0 -> v5.4.1 (PATCH).

v5.2.3 — Security Audit & Critical Bug Fix Round (PATCH)
----------------------------------------------------------------------------------
### Root Cause Analysis (RCA):
- **[CRITICAL] `existingTx` ReferenceError:** `payments.ts` line 262 referenced `existingTx` instead of `previousStatus` — caused silent crash in manual transaction verify path.
- **[CRITICAL] Points double-spend:** `payments.ts` non-atomic points deduction — concurrent requests could spend same points twice.
- **[CRITICAL] `updateSellerBalances` non-idempotent:** Called multiple times (iPaymu callback + auto-cleanup) could duplicate seller balance credits.
- **[HIGH] TOCTOU race in stock restore:** `stock.js` SELECT-then-UPDATE pattern allowed concurrent restores to phantom stock.
- **[HIGH] `total_amount` permanently mutated by partial points:** Partial points payment reduced `total_amount` in DB — skewed financial reports forever.
- **[MEDIUM] Missing auth on critical endpoints:** `/api/digital/deposit` (open to all), `/api/digital/order` (open), `/api/transactions/cancel` (no ownership check).
- **[MEDIUM] Quantity validation bypass:** Cart store allowed negative/NaN quantities via URL manipulation.
- **[LOW] Misleading `getUserId` function name:** Actually returns auth token, not user ID — confusion risk.
- **[LOW] API key prefix leaked in error logs:** Digiflazz `bc` prefix exposed in console.error.

### Perbaikan:
- **[FIX] `src/routes/payments.ts` (existingTx):** Hapus referensi `existingTx`, pakai `previousStatus` dari destructured `txRecord`.
- **[FIX] `src/routes/payments.ts` (points double-spend):** Atomic deduction via `.gte("loyalty_points", amount)` + `.select()` validation — rollback if saldo changed.
- **[FIX] `src/services/payment.js`:** Idempotency guard with `metadata->>balances_updated` flag — skip if already credited.
- **[FIX] `src/services/stock.js`:** TOCTOU fix on `deductTransactionStock` — atomic conditional update `.filter('metadata->>stock_restored', 'eq', 'true')` before restore.
- **[FIX] `src/services/stock.js`:** TOCTOU fix on `restoreTransactionStock` — atomic claim via `.filter('metadata->>stock_restored', 'neq', 'true')` cegah double-restore.
- **[FIX] `src/routes/payments.ts` (partial points):** Simpan discount di metadata only — `total_amount` tidak diubah permanen.
- **[FIX] `src/routes/digital.ts`:** Tambah auth guard `/api/digital/deposit` (admin-only) + `/api/digital/order` (auth required). Hapus API key prefix dari log.
- **[FIX] `src/routes/transactions.ts`:** Cancel endpoint — ownership check jika auth header ada (guest path unchanged).
- **[FIX] `src/store/useCartStore.ts`:** Quantity clamp `Math.max(0, Math.floor(Number(quantity) || 0))`.
- **[FIX] `server.ts`, withdrawals.ts, stock.ts, productReturns.ts:** Rename `getUserId` → `getToken`.
- **[FIX] `src/routes/withdrawals.ts`:** Guard status terlalu strict (`!== "pending"`) — block flow `approved→paid`. Ganti jadi cek terminal state (`paid`/`rejected`).
- **[FIX] `src/services/background-jobs.js`:** Hapus `.is("receipt_image", null)` dari auto-cleanup — transaksi dengan receipt AI-tolak tidak pernah di-cleanup. Ganti: hanya skip jika receipt_image ada & belum pernah gagal verifikasi.
- **[FIX] `src/services/stock.js` (restoreTransactionStock):** Guard `!tx?.metadata?.stock_deducted` terlalu ketat — metadata mungkin gagal di-set (`undefined`). Ganti `=== false` agar undefined tetap proceed ke fallback `transaction_items`.
- **[NEW] `fix_lost_stock.sql`:** Recovery script untuk mengembalikan stock yang terlanjur hilang akibat bug auto-cleanup. Jalankan di Supabase SQL Editor.

### Teknis:
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx, PortalLayout.tsx:** v5.2.2 → v5.2.3 (PATCH: security audit & critical bug fixes).

v5.2.2 — Seller Balance Correction — Retroactive Fix (PATCH)
----------------------------------------------------------------------------------
### Root Cause Analysis (RCA):
- **[RCA] `updateSellerBalances()` bug:** Old code used `item.price` (unit price) instead of `item.subtotal` (net after 8% fee) for balance credit. Also used unit price (not `price * quantity`) for `total_sales`. Erroneous 0.7% MDR deduction applied on top of 8% platform fee.
- **[RCA] `total_withdrawn` double-count on Fauzan:** Withdrawal approval process likely called multiple times for same withdrawals, inflating `total_withdrawn` from Rp 722.000 to Rp 1.341.960.

### Perbaikan:
- **[FIX] Balance corrected to actual earnings (6 sellers):** Set balance = total subtotal from paid transactions - actual withdrawals for each seller.
  - Fauzan: 108.030 → **157.584** (+49.554)
  - Hidayatullah: 982 → **23.240** (+22.258)
  - Admin Sariroti: 112.125 → **161.250** (+49.125)
  - Dadang Hardito: 143.520 → **145.360** (+1.840)
  - Alif Irfansyah: 976.830 → **8.520** (-968.310)
  - Nurul Hasanah: 186.698 → **189.300** (+2.602)
- **[FIX] `total_sales` & `total_fee_paid` corrected** to match actual gross/net from paid transactions.
- **[FIX] Fauzan `total_withdrawn` fixed:** 1.341.960 → 722.000 (matches actual withdrawal records).

### Teknis:
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx, PortalLayout.tsx:** v5.2.1 → v5.2.2 (PATCH: koreksi balance seller retroaktif).

v5.2.1 — Stock Accuracy Fix — RCAs & Optimistic Locking (PATCH)
----------------------------------------------------------------------------------
### Root Cause Analysis (RCA):
- **[RCA] 6 stock mutation paths ditemukan:** Sale deduction (transactions.ts), stock restore (stock.js), restock approval (stock.ts), return approval (productReturns.ts), stock opname (AdminStockOpname.tsx), iPaymu callback (payments.ts).
- **[RCA] Race condition pada SELECT→UPDATE:** Semua path menggunakan pola non-atomik `SELECT stock → compute → UPDATE stock` → overselling & phantom stock.
- **[RCA] Partial deduction lost:** Jika sebagian item gagal deduct, `stock_deducted = false` → `restoreTransactionStock` skip SELURUH items → stock yang sudah deduct tidak pernah dikembalikan.
- **[RCA] Double restore race:** Guard `stock_restored` dibaca lalu di-set secara non-atomik → dua caller bisa restore bersamaan → phantom stock.
- **[RCA] iPaymu + auto-cleanup race:** Auto-cleanup restore stock lalu iPaymu callback set status "paid" tanpa re-deduct → phantom stock permanen.

### Perbaikan:
- **[FIX] `src/services/stock.js`:** Rewrite `restoreTransactionStock` — pakai `deducted_products` dari metadata (per-item) + optimistic locking `(stock = oldStock)` pada UPDATE. Tambah `deductTransactionStock()` untuk re-deduct setelah auto-cleanup.
- **[FIX] `src/routes/transactions.ts`:** Deduksi stok sekarang atomic — UPDATE dengan `WHERE stock = oldStock`. Simpan `deducted_products` map per-item (produk ID → quantity + seller_id) di metadata. Partial deduction tetap tercatat untuk restore.
- **[FIX] `src/routes/payments.ts`:** iPaymu SUCCESS callback — jika `stock_restored = true` (auto-cleanup sudah restore), panggil `deductTransactionStock()` untuk re-deduct. iPaymu FAILURE callback — sederhanakan, hapus guard redundant (handle di `restoreTransactionStock`).
- **[FIX] `src/routes/stock.ts`:** Restock approval — optimistic locking `(stock = oldStock)`.
- **[FIX] `src/routes/productReturns.ts`:** Return approval — optimistic locking + hapus `Math.max(0, ...)` yang silent data loss.
- **[FIX] `src/pages/dashboard/admin/AdminStockOpname.tsx`:** Stock opname — optimistic locking, tolak jika stock sudah berubah sejak dimuat.
- **[FIX] `src/routes/admin.ts`:** Stock report — tetap pakai dual-query untuk initialStock akurat.

### Teknis:
- **[CHANGE] Home.tsx, PortalLayout.tsx:** v5.2.0 → v5.2.1 (PATCH: perbaikan akurasi stok).

v5.2.0 — Route Refactor, Order Tracking, Analytics, WA Notif (MINOR)
----------------------------------------------------------------------------------
### Refactor Arsitektur:
- **[REFACTOR] server.ts Modularisasi Tahap 1:** Ekstrak 7 service modules — `notification.ts`, `stock.ts`, `email.ts`, `payment.ts`, `digiflazz.ts`, `background-jobs.ts`, `wa-notification.ts`.
- **[REFACTOR] server.ts Modularisasi Tahap 2:** Ekstrak 13 route modules — `analytics.ts`, `push.ts`, `diagnostics.ts`, `misc.ts`, `auth.ts`, `portal.ts`, `digital.ts`, `payments.ts`, `transactions.ts`, `admin.ts` + existing `withdrawals.ts`, `stock.ts`, `productReturns.ts`.
- **[CHANGE] server.ts:** ~5000 baris → 678 baris (skrg ~650 baris setelah cleanup). Entry point bersih: imports, middleware, route registrations, SPA fallback.
- **[CLEANUP] server.ts:** Hapus 50+ baris komentar stale `// xxx moved to src/routes/xxx.ts`, hapus komentar redundan.
- **[CLEANUP] AGENTS.md:** Update daftar dead code — konfirmasi 6 item sudah tidak ada di repo.

### Fitur Baru:
- **[FEAT] Order Tracking:** Flow status `paid → processed → ready → completed` dengan 3 endpoint baru + push/WA notification setiap transisi.
- **[FEAT] Analytics Dashboard:** `/dashboard/admin/analytics` dan `/dashboard/seller/analytics` — revenue chart, top products, category pie (Recharts).
- **[FEAT] WhatsApp Notification:** Service `wa-notification.js` via wa.me link. Tersimpan di `notification_logs`.
- **[FEAT] Auto-Restock Alert:** `checkLowStockAndNotify` → push + WA + log saat stok < 5.

### Perbaikan:
- **[FIX] PortalLayout:** Tambah version display di sidebar.
- **[FIX] AdminUnionPrograms:** Bare `.catch(console.error)` → error handler informatif.
- **[FIX] email.js:** `sendSarirotiEmailInternal` return type → `{ success, data/error }` (backward compat).
- **[FIX] triggerSarirotiEmail:** Full HTML email template di email.js (seperti versi inline sebelumnya).
- **[FIX] server.ts:** Tambah `GET /api/test-ping` health check endpoint.
- **[FIX] server.ts:** Hapus duplikasi `updateDigiflazzCache` (import & definisi lokal) — resolve TDZ di Node v20.

### Optimasi Build:
- **[OPTIM] vendor chunk splitting:** Pisah recharts, xlsx, html2canvas, motion, tiptap editor, scanner deps ke chunk terpisah via `manualChunks`. Main bundle turun 780kB → 642kB.
- **[DELETE] `@sentry/vite-plugin`:** Dead dependency — tidak dipakai di mana pun.

### Infrastruktur:
- **[IMPROVE] `scripts/deploy-vps.ps1`:** Tambah `pm2 flush` sebelum restart — log error restart sebelumnya tidak mengganggu health check. Tambah SCP `src/services/` yang sebelumnya terlewat.


### Perubahan Lain:
- **[CHANGE] Version:** package.json, Home, DashboardLayout, PortalLayout → v5.2.0

v5.1.0 — Security Audit & QA Fixes (PATCH)
----------------------------------------------------------------------------------
### Perbaikan Keamanan (CRITICAL):
- **[FIX] API 404 Catch-All:** Pindahkan `app.use('/api/*')` ke LUAR blok `if (!process.env.VERCEL)` — sekarang return JSON 404 di Vercel juga, mencegah "Unexpected token '<'" saat VPS failover aktif.
- **[FIX] RLS Policies:** Migration `migration_v5_1_0_rls_security_fix.sql`:
  - `transactions_update_public` → owner/admin only
  - `transactions_insert_public` → buyer_id harus = auth.uid()
  - `stock_reservations_public_all` → split ke select/insert/update/delete per user
  - `notifications_insert_all` → self or admin only
  - `failed_tx_insert_public` → harus punya transaksi terkait
  - `get_public_profile()` function untuk akses profil terbatas

### Perbaikan Lain:
- **[FIX] Version:** Home.tsx → v5.0.0 dari v4.22.0 (konsisten dengan package.json)
- **[FIX] Scanner Route:** `App.tsx` → ganti `path="scanner"` → `path="admin/scanner"` (konsisten dengan route admin lain)
- **[FIX] Hapus Duplicate Route:** Hapus `admin/programs` (duplikat dari `admin/union-programs`)
- **[FIX] Sensitive Info Leak:** Hapus console.log yang bocorkan VA, API key length, reset token, session token di ipaymu/client.ts, ResetPasswordPage.tsx, AdminSellers.tsx
- **[FIX] Aksesibilitas:** Tambah `htmlFor`/`id` di form login + `aria-label` di tombol sidebar DashboardLayout
- **[CHANGE] package.json:** v5.0.0 → v5.1.0 (PATCH: perbaikan keamanan & bug)

v5.0.0 — Doorprize Terpadu + Multi-Source + Prize Sequence (MAJOR)
----------------------------------------------------------------------------------
### Perubahan Arsitektur:
- **[MAJOR] Doorprize Terpadu:** Menggabungkan `AdminDoorprize.tsx` (undian sederhana) dan `AdminDoorprizeSpin.tsx` (spin + animasi + audio) menjadi 1 halaman adaptif. Hapus `AdminDoorprizeSpin.tsx` (permanen).
- **[FEAT] 3 Sumber Peserta:** (1) Kupon Program Serikat (`program_coupons` via `gate_type='doorprize'`), (2) Input Manual (Nama + NIK), (3) Upload Excel (.xlsx dengan kolom Name/NAMA + NIK).
- **[FEAT] Prize Sequence:** Admin bisa menambahkan banyak hadiah (nama + quantity) dalam urutan dari terkecil ke terbesar. Undian berjalan otomatis per prize. Satu klik = satu undian per prize.
- **[FEAT] Spin Animation:** Slot-machine style dari AdminDoorprizeSpin.tsx — nama berputar cepat lalu melambat. Efek suara Web Audio (tick + win fanfare).
- **[FEAT] "Wah" Winner Reveal:** Animasi confetti (40 partikel warna-warni via motion), teks gradient besar, badge "Pemenang" dengan gradient, tombol "Simpan Gambar" via html2canvas (save sebagai PNG).
- **[FEAT] Anti-Duplikat:** Filter otomatis pemenang sebelumnya (by NIK) agar tidak menang 2x dalam sesi yang sama.
- **[FEAT] Export Excel:** Rekap semua pemenang dengan kolom No, Hadiah, Nama, NIK, Waktu Undian.
- **[CHANGE] Sidebar:** Hapus "Spin Doorprize" — hanya tersisa "Undian Doorprize".
- **[DELETE]** `AdminDoorprizeSpin.tsx` — tidak lagi digunakan.

### Teknis:
- **[CHANGE] package.json:** v4.23.0 → v5.0.0 (MAJOR: restrukturasi besar doorprize).

v4.23.0 — Survey Report, Close Program, Loyalty 0.8% + Partial + Expiry
----------------------------------------------------------------------------------
### Fitur Baru:
- **[FEAT] `AdminSurveyReports.tsx`:** Halaman baru untuk lihat hasil survei (aggregasi jawaban per pertanyaan + detail per responden). Tersedia di sidebar → Laporan & Evaluasi → Hasil Survei.
- **[FEAT] `AdminUnionPrograms.tsx`:** Tombol "Tutup" untuk program yang lewat end_date → otomatis nonaktifkan program + expire semua kupon aktif.
- **[FEAT] `server.ts`:** Endpoint `POST /api/admin/programs/:programId/close` — close program & expire coupons.
- **[FEAT] `PortalProgram.tsx`:** Program yang sudah selesai tetap muncul di history user. Kupon expired ditampilkan dengan status "Expired".
- **[FEAT] Loyalty revamp:** 0.8% earning, partial payment (poin + metode lain), FIFO expiry 12 bulan, tracking via table `points_history`, min redeem 1.000 pts.
- **[FEAT] `server.ts`:** Endpoints baru — `/api/payment/points/partial-pay`, `/api/portal/points/balance`, `/api/portal/points/history`.
- **[FEAT] `Checkout.tsx`:** Opsi bayar parsial dengan poin (input jumlah poin, sisa bayar via metode lain).
- **[FEAT] `Profile.tsx`:** Riwayat poin (earned/spent/expired) dengan tanggal expiry.

### Teknis:
- **[CHANGE] `migration_v4_20_0.sql`:** Migration file — alter program_coupons status allow 'expired', create points_history table + functions.
- **[CHANGE] package.json:** v4.22.1 → v4.23.0 (MINOR: fitur baru aman).

v4.22.1 — Fix: Vote & survey terduplikasi saat edit pengumuman gathering
----------------------------------------------------------------------------------
### Perbaikan:
- **[FIX] `AdminAnnouncements.tsx`:** Saat edit pengumuman gathering, kandidat di-`DELETE`+`INSERT` ulang dengan UUID baru → vote jadi orphan (hilang referensi). Diperbaiki dengan hanya `UPDATE` kandidat existing (pertahankan ID), `INSERT` kandidat baru, dan `DELETE` hanya kandidat yang dihapus beserta vote-nya. Vote lama tetap utuh.
- **[FIX] `GatheringSurveys.tsx`:** User bisa submit survei berkali-kali karena tidak ada pengecekan duplikat. Ditambahkan `useEffect` yang mengecek `announcement_survey_responses` saat mount — jika sudah pernah submit, tampilkan status "sudah tercatat" dan disable form.

### Teknis:
- **[CHANGE] package.json:** v4.22.0 → v4.22.1 (PATCH: perbaikan duplikasi data).

v4.22.0 — Auto-Failover: VPS mati otomatis pindah ke Vercel
----------------------------------------------------------------------------------
### Fitur Baru:
- **[FEAT] `workers/failover.js`:** Cloudflare Worker untuk auto-routing. Di `api.spscorner.store`, Worker cek health VPS tiap 30 detik. Kalau VPS hidup → proxy ke VPS. Kalau mati → fallback ke Vercel (`www.spscorner.store`).
- **[FEAT] `src/lib/api.ts`:** Fetch wrapper dengan 2-layer failover:
  1. Coba `api.spscorner.store` (Worker → VPS/Vercel)
  2. Kalau Worker unreachable, fallback ke relative path (Vercel serverless langsung)
- **[FEAT] `src/main.tsx`:** Patch global `window.fetch` — semua `/api/*` request otomatis kena failover tanpa perlu ubah 65+ file.

### Arsitektur Failover:
```
Frontend (/api/...)
  ├── Layer 1: api.spscorner.store (Cloudflare Worker)
  │     ├── VPS hidup → proxy ke VPS (45.158.126.76:3000)
  │     └── VPS mati → proxy ke www.spscorner.store (Vercel serverless)
  └── Layer 2: Worker unreachable → fallback ke relative path (Vercel direct)
```

### Perbaikan:
- **[FIX] `server.ts`:** Tambah import `HttpsProxyAgent` dari `https-proxy-agent` yang hilang (Fixie error).
- **[FIX] `api/index.ts`:** Fix Vercel serverless function handler — restore original URL dari rewrite query param `__api`.
- **[FIX] `vercel.json`:** Tambah rewrite `/api/(.*)` → `/api?__api=$1` agar serverless function menangani semua sub-path API.
- **[DELETE] `api/[...slug].ts`:** Dihapus — catch-all pattern tidak berfungsi di Vercel standard serverless.
- **[ENV] `.env`:** Isi `FIXIE_URL` untuk Fixie proxy.

### Teknis:
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx:** v4.21.1 → v4.22.0 (MINOR: fitur baru auto-failover).

v4.21.1 — Fitur Pilihan NIK (Checkbox) Laporan Kupon
----------------------------------------------------------------------------------
### Fitur Baru:
- **[FEAT] `AdminCouponReports.tsx`:** Tambah fitur checkbox pilihan NIK untuk laporan kupon. Admin dapat mencentang/memilih NIK mana saja yang ingin disertakan ke dalam ekspor PDF maupun Excel, dengan indikator status dinamis "Terpilih X dari Y data".

v4.21.0 — Optimasi Database, Pengamanan RLS, dan Pembersihan Teknis
----------------------------------------------------------------------------------
### Perbaikan Utama:
- **[FIX] `server.ts` (Auth Middleware):** Perbaikan security dan race condition pada `checkAccess` untuk role `member` (jenis_anggota).
  - Sebelumnya: Query terpisah dan manipulasi state di level middleware → tidak aman saat concurrency tinggi.
  - Sekarang: Query tunggal `getMemberRole(user.id)` via JOIN dengan `user_members` → hanya satu query per request.
  - Menghapus flag `isModerator` dari auth state — sekarang ditentukan langsung dari `user_members.role` saat autentikasi (1 query instead of 2).
  - Mengurangi total query per request dari 2 → 1, dengan konsistensi data yang lebih baik.

- **[FIX] `server.ts` (Transaction Logic):** Perbaikan race condition di `POST /api/transaction/sell` dan `POST /api/transaction/buy`.
  - Sebelumnya: `stock.decrement()` dan `balance.decrement()` dilakukan di level middleware setelah authorization, bukan di dalam transaction block.
  - Sekarang: Semua operasi database critical (stock decrement, balance decrement) dipindahkan ke dalam database transaction menggunakan `client.tx()`.
  - Menambahkan `FOR UPDATE` clause untuk mencegah lost updates saat high concurrency.
  - Stock yang "dipinjam" otomatis dikembalikan jika gagal di tengah proses (rollback), mencegah over-selling atau stock negatif.

- **[FIX] `server.ts` (Catalog Logic):** Perbaikan race condition di `POST /api/seller/products/publish`.
  - Menambahkan `FOR UPDATE` di query update stock.

- **[FIX] `server.ts` (Security):** Mengganti username/password PostgreSQL dengan environment variables `SUPABASE_DB_PASSWORD` dan `SUPABASE_DB_USERNAME`.
  - Menggunakan client dengan SSL/TLS强制, mematikan `rejectUnauthorized` untuk kompatibilitas SSL-on-demand provider.

- **[FIX] `server.ts` (Error Handling):** Memperbaiki parse error untuk JSON dari external APIs (Pusher, Stripe, iPaymu) dengan menambahkan `try/catch`.

### Refactoring & Cleanup:
- **[Refactor] `server.ts`:** Menghapus redundant code, menyederhanakan endpoint, mengkonsolidasikan logic authentication, dan memisahkan middleware auth dari business logic.
- **[Refactor] `DashboardLayout.tsx`:** Menghapus duplikat state `member` yang mengarah ke multiple queries.
- **[Delete] `clean.bat`:** Dihapus karena tidak digunakan.

### Teknis:
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx:** v4.20.0 → v4.21.0 (PATCH: critical race condition fixes & security improvements).

v4.20.1 — Fix Sentry startup crash & clean up layout version display
-----------------------------------------------------------------------
### Perbaikan:
- **[FIX] `server.ts`:** Hapus middleware `Sentry.Handlers.requestHandler()` & `tracingHandler()` yang deprecated, dan ganti `app.use(Sentry.Handlers.errorHandler())` dengan `Sentry.setupExpressErrorHandler(app)` untuk kompatibilitas penuh dengan Sentry SDK Node v10. Mencegah crash startup server.
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx:** Update versi ke `v4.20.1` (PATCH: perbaikan crash startup).

v4.20.0 — Pengumuman Gathering: Voting Ketua Panitia, Survei, Share WhatsApp & Target NIK
----------------------------------------------------------------------------------
### Fitur Baru:
- **[FEAT] `PortalPengumuman.tsx`:** Pengumuman tipe "Gathering" — mendukung voting kandidat ketua panitia dengan foto, link survei internal (dynamic forms), dan share ke WhatsApp.
- **[FEAT] `AdminAnnouncements.tsx`:** Admin bisa membuat pengumuman Gathering: pilih tipe (Umum/Gathering), kelola kandidat (nama + foto), tambah survei, target NIK, toggle voting, dan deadline voting.
- **[NEW] `GatheringVoting.tsx`:** Komponen voting kandidat — card grid dengan foto, bar chart hasil real-time (hanya terlihat setelah user vote), bisa ubah pilihan, access control berbasis NIK.
- **[NEW] `GatheringSurveys.tsx`:** Komponen link survei — integrasi dengan dynamic forms internal, access control berbasis NIK.
- **[NEW] `WhatsAppShare.tsx`:** Tombol share pengumuman ke WhatsApp — generate link wa.me dengan judul pengumuman dan URL portal.
- **[DB] Migration SQL:** Tabel baru `gathering_candidates` dan `gathering_votes`, kolom baru di `announcements` (`announcement_type`, `gathering_config`, `target_niks`) + RLS policies.

### Pengaturan Akses:
- Semua anggota bisa melihat pengumuman Gathering.
- Hanya NIK yang ditargetkan oleh admin yang bisa ikut voting dan mengakses survei.
- Voting: 1 user = 1 vote, bisa diubah sebelum deadline.
- Hasil voting (bar chart persentase) hanya terlihat setelah user sudah memilih.

### Teknis:
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx:** v4.19.0 → v4.20.0 (MINOR: fitur baru Gathering).

v4.19.0 — Tambah input manual data scan, download PDF langsung, & export Excel fix
----------------------------------------------------------------------------------
### Fitur Baru:
- **[FEAT] `AdminCouponReports.tsx`:** Tambah modal "Tambah Data Scan" untuk input manual data scan kupon (NIK, Nama, Waktu Scan, Jenis Kupon) — memungkinkan admin mencatat klaim kupon secara retroaktif untuk acara-acara yang tidak terekam scan.
- **[FEAT] `AdminCouponReports.tsx`:** Ganti "Cetak PDF Resmi" (window.print/print dialog) → "Download PDF" langsung via jsPDF + autotable. Unduh file PDF dengan kop surat resmi, tabel multi-page dengan header berulang, tanpa dialog print browser.

### Perbaikan:
- **[CHANGE] `AdminCouponReports.tsx`:** Default tanggal diubah dari hari ini → awal bulan sampai hari ini.
- **[FIX] `AdminCouponReports.tsx`:** Export Excel crash karena `coupon_type` undefined — tambah fallback `(row.coupon_type || row.gate_type || '-')`.
- **[FIX] `AdminCouponReports.tsx`:** Edit waktu inline sekarang menyertakan detik (step=1, slice(0,19)).

### Teknis:
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx:** v4.18.2 → v4.19.0 (MINOR).
- **[NEW] Dependensi:** `jspdf` + `jspdf-autotable` untuk generate PDF client-side.

v4.18.2 — Fix iPaymu callback status not updating & rate limiter crash (X-Forwarded-For)
-----------------------------------------------------------------------
### Perbaikan:
- **[FIX] `server.ts`:** iPaymu callback handler — status matching sekarang case-insensitive + handle variasi status (`success`, `sukses`, `completed`, `settlement`, `fail`, `expired`, `deny`, `cancel`) agar callback otomatis iPaymu tidak ter-mapping ke "pending".
- **[FIX] `server.ts`:** Tambah fallback lookup transaksi via `payment_details->ipaymu_trx_id` jika `reference_id` tidak ditemukan di database.
- **[FIX] `server.ts`:** Ganti `.single()` → `.maybeSingle()` agar tidak throw error jika transaksi tidak ditemukan.
- **[FIX] `server.ts`:** Ekstraksi field callback lebih fleksibel (camelCase, uppercase fallback).
- **[FIX] `server.ts`:** Tambah `app.set("trust proxy", 1)` — mencegah crash `express-rate-limit` karena `X-Forwarded-For` header di belakang Vercel reverse proxy.

### Teknis:
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx:** v4.18.1 → v4.18.2 (PATCH: perbaikan callback iPaymu + trust proxy).

v4.18.1 — Fix API 404, stock restore on admin reject & CI/CD auto-deploy
-----------------------------------------------------------------------
### Perbaikan:
- **[FIX] `server.ts`:** Tambah `app.use('/api/*')` catch-all middleware sebelum SPA fallback — mengembalikan JSON 404 untuk endpoint API yang tidak terdaftar, bukan HTML. Mencegah "Unexpected token '<'" pada `res.json()`.
- **[FIX] `server.ts`:** Tambah endpoint `POST /api/admin/transactions/reject` — sebelumnya tidak ada, admin reject langsung tanpa restore stock. Sekarang reject = restore stock + set status failed.
- **[FIX] `AdminDashboard.tsx`:** Hapus fallback `supabase.update({ status: "failed" })` tanpa restore stock. Sekarang hanya pakai endpoint backend yang proper.
- **[FIX] `AdminDashboard.tsx`:** Total pendapatan sekarang pakai filter `.in('status', ['success', 'paid'])` agar sinkron dengan breakdown seller.
- **[FIX] `SellerProducts.tsx`:** Ubah `res.json()` → `res.text()` + `JSON.parse()` dengan try/catch agar error non-JSON tidak memunculkan pesan mentah.
- **[FIX] `SellerTransactions.tsx`:** Sama, `res.json()` → `res.text()` + `JSON.parse()`.

### Fitur Baru:
- **[NEW] `database/migrations/001_stock_rpc.sql`:** Fungsi RPC `adjust_stock_rpc` dengan `SELECT ... FOR UPDATE` (row-level locking) + kolom `transaction_id` di `stock_adjustments` untuk traceability.
- **[NEW] Stock retry mechanism:** `atomicAdjustStock()` — priority 1: RPC (FOR UPDATE), priority 2: optimistic locking + retry 3× dengan delay progresif.
- **[NEW] Rekonsiliasi stok otomatis:** Job tiap 30 menit bandingkan `currentStock` vs `expectedStock` dari trail adjustment, notifikasi admin jika gap ≥5 unit.
- **[NEW] `GET /api/stock-movements/:productId`:** Endpoint backend untuk riwayat mutasi stok per produk.
- **[NEW] Halaman Lacak Riwayat Stok:** Fitur traceability per produk — filter tanggal, tipe mutasi (terjual/restock/koreksi/opname), link transaksi, status transaksi. Admin lihat semua produk, Seller lihat produknya sendiri.
- **[NEW] Navigasi sidebar:** Admin → "Lacak Riwayat Stok", Seller → "Riwayat Stok".

### Perubahan Keamanan Stok:
- **[CHANGE] `AdminProducts.tsx`:** Field stok di form EDIT produk dihapus (read-only). Stok hanya bisa diubah via Restock Request, Retur, atau Stock Opname. Stok awal tetap bisa diisi saat CREATE produk.

### Infrastructure:
- **[NEW] `scripts/deploy-vps.ps1`:** Deploy script all-in-one (lint → build → SCP → npm install → restart → verify).
- **[NEW] `scripts/auto-deploy.sh`:** Script auto-deploy di VPS via cron (git pull setiap 5 menit).
- **[NEW] Cron job VPS:** `*/5 * * * *` auto-deploy — deteksi commit baru, install deps jika berubah, restart PM2, health check.
- **[DELETE] `push_to_github.bat`:** Diganti `scripts/deploy-vps.ps1`.

### Teknis:
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx:** v4.18.0 → v4.18.1 (PATCH: perbaikan error handling & stock).
- **[UPDATE] AGENTS.md:** Arsitektur VPS+Vercel, deploy checklist, auto-deploy cron, gotcha API 404 + @sentry/node.

v5.3.1 — Department Targeting untuk Pengumuman Gathering (PATCH)
----------------------------------------------------------------------------------
### Fitur Baru:

**🎯 Target Departemen untuk Voting & Survei:**
- **[FEAT] Department Multi-Select:** Di form pengumuman gathering, admin bisa pilih departemen tujuan — semua NIK dari departemen tsb otomatis ditambahkan ke target.
- **[FEAT] Resolve otomatis:** Saat simpan, sistem resolve departemen → NIK dari tabel `employees`, merge dengan NIK manual, simpan di `target_niks` + `target_departments`.
- **[FEAT] UI Picker:** Tombol-tombol departemen dengan toggle visual (ungu active / abu-abu inactive), jumlah departemen terpilih.

**👁️ Akses Otomatis untuk Anggota:**
- **[FEAT] PortalPengumuman:** Fetch departemen user dari `employees`, teruskan ke komponen voting & survei.
- **[FEAT] GatheringVoting/GatheringSurveys:** Cek akses via NIK ATAU departemen — anggota departemen target otomatis bisa vote/isi survei.

### Migrasi SQL (WAJIB):
Jalankan di Supabase SQL Editor:
1. `database/migrations/003_announcement_department_target.sql` — tambah kolom `target_departments`

### Perubahan File:
- **[CHANGE] src/pages/dashboard/admin/AdminAnnouncements.tsx:** Tambah state, fetch, UI picker department, resolve NIK saat save, edit populate
- **[CHANGE] src/pages/portal/PortalPengumuman.tsx:** Fetch user department, pass props ke child komponen
- **[CHANGE] src/components/portal/GatheringVoting.tsx:** Props baru `targetDepartments` + `userDepartment`, logika akses OR (NIK/department)
- **[CHANGE] src/components/portal/GatheringSurveys.tsx:** Props baru `targetDepartments` + `userDepartment`, logika akses OR (NIK/department)
- **[NEW] database/migrations/003_announcement_department_target.sql:** Migration kolom target_departments
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx, PortalLayout.tsx:** v5.3.0 → v5.3.1 (PATCH)

v5.4.0 — AI Form Builder, Image Field, Departemen Targeting & Form Reporting (MINOR)
----------------------------------------------------------------------------------
### Fitur Baru:

**🤖 AI Form Builder (Groq):**
- **[FEAT] Generate dengan AI:** Tombol "AI" di form builder — admin cukup deskripsikan form yang diinginkan, AI (Groq/Llama) generate field-field secara otomatis.
- **[FEAT] Endpoint `/api/ai/generate-form`:** Backend endpoint pakai `groq-sdk` yang sudah ada, system prompt menginstruksikan format JSON field.
- **[FEAT] Smart Append:** Hasil AI ditambahkan ke field existing, admin bisa edit/tambah/hapus manual.

**🖼️ Image Field Type:**
- **[FEAT] Tipe field 'image':** Field khusus upload gambar — support upload ke Supabase Storage ATAU input URL.
- **[FEAT] dual input:** Upload file (image/*) + divider "ATAU" + input URL dengan tombol link.
- **[FEAT] Preview & Hapus:** Setelah upload, tampilkan preview 80px crop dengan tombol hapus.

**🎯 Target NIK + Departemen di Form Builder:**
- **[FEAT] Target NIK:** Textarea NIK manual (sama seperti announcement/program).
- **[FEAT] Target Departemen:** Picker multi-select departemen, resolve otomatis ke NIK saat simpan.
- **[FEAT] Portal Access Check:** PortalFormView cek NIK/departemen user — jika tidak cocok tampilkan "Akses Terbatas".
- **[DB] Migration 004:** Kolom `target_niks` (text[]) dan `target_departments` (text[]) di `dynamic_forms`.

**🎯 Target Departemen di Program Serikat:**
- **[FEAT] AdminUnionPrograms:** Tambah picker departemen + resolve NIK. NIK dari departemen di-merge dengan NIK manual saat simpan.
- **[DB] Migration 004:** Kolom `target_departments` (text[]) di `union_programs`.

**📊 Form Reporting Lengkap:**
- **[FEAT] Grafik Agregasi:** Bar chart horizontal per field (radio/select/checkbox/rating/scale/image_choice) dengan persentase.
- **[FEAT] Export PDF:** Download PDF via jsPDF + autotable — tabel multi-page dengan header berulang.
- **[FEAT] Filter Departemen:** Dropdown filter departemen (data dari tabel employees).
- **[FEAT] Image Preview di Detail:** Jawaban tipe image ditampilkan sebagai thumbnail 80px; file_upload sebagai link download.
- **[FEAT] Smooth Animasi:** Grafik muncul dengan spring animation, bar width animasi progresif.

### Migrasi SQL (WAJIB):
Jalankan di Supabase SQL Editor:
1. `database/migrations/004_form_image_target.sql` — tambah kolom target_niks & target_departments

### Perubahan File:
- **[CHANGE] src/types/form.ts:** Tambah tipe 'image' ke FieldType union
- **[NEW] src/routes/misc.ts:** Endpoint POST /api/ai/generate-form (Groq integration)
- **[CHANGE] src/pages/dashboard/admin/AdminFormBuilder.tsx:** AI modal, image field type, target NIK + departemen picker, save resolve
- **[CHANGE] src/pages/portal/PortalFormView.tsx:** Image field upload/URL, targeting check, user department fetch
- **[CHANGE] src/pages/dashboard/admin/AdminFormResponses.tsx:** Grafik agregasi, export PDF, filter departemen, renderAnswer image/file
- **[CHANGE] src/pages/dashboard/admin/AdminUnionPrograms.tsx:** Departemen picker + resolve NIK di eligibility
- **[NEW] database/migrations/004_form_image_target.sql:** Migration kolom baru
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx, PortalLayout.tsx:** v5.3.1 → v5.4.0 (MINOR)

v5.3.1 — Department Targeting untuk Pengumuman Gathering (PATCH)
----------------------------------------------------------------------------------
### Fitur Baru:

**🧩 Form Conditional Logic (Branching/If-Else):**
- **[FEAT] Conditional Logic:** Setiap field di form builder bisa punya aturan "Tampilkan jika [field acuan] = [nilai]". Admin pilih field acuan, operator (= / ≠ / ∈), dan nilai pemicu.
- **[FEAT] Smooth Animation:** Field yang disembunyikan/muncul menggunakan spring animation — collapse/expand halus, tidak melompat.
- **[FEAT] Visual Indicator:** Preview mode menampilkan badge ungu "Tampil jika ..." agar admin tahu aturannya tanpa perlu expand.
- **[FEAT] Smart Value Selector:** Jika field acuan punya opsi (radio/select/checkbox), otomatis dropdown pilihan nilai.
- **[FEAT] Hidden Field Exclusion:** Field tersembunyi: (1) tidak divalidasi, (2) tidak dikirim di payload, (3) animasi smooth collapse.

**👥 Employee Master Database (NIK → Nama → Departemen):**
- **[FEAT] Database Karyawan:** Tabel `employees` dengan NIK (unique), Nama, Departemen. Admin kelola via "Input Data Induk → Database Karyawan".
- **[FEAT] AdminEmployees.tsx:** CRUD lengkap — search, import Excel, download template.
- **[FEAT] Auto-Sync Nama:** Register / Google OAuth / Edit profil — jika NIK cocok di `employees`, nama otomatis pakai data master. Nama aneh dari Google tidak kepakai.
- **[FEAT] Foundation Filter Departemen:** Data departemen siap digunakan untuk filtering pengumuman dll.

### Migrasi SQL (WAJIB):
Jalankan di Supabase SQL Editor:
1. `database/migrations/002_employee_master_data.sql` — buat tabel `employees`

### Perubahan File:
- **[CHANGE] src/types/form.ts:** Tambah `Condition` interface + field `condition?` di `FormField`
- **[CHANGE] src/pages/dashboard/admin/AdminFormBuilder.tsx:** UI "Atur Logika Tampil" editor + preview badge
- **[CHANGE] src/pages/portal/PortalFormView.tsx:** Fungsi `isFieldVisible`, animasi spring, validasi & payload filter
- **[NEW] database/migrations/002_employee_master_data.sql:** Migration tabel employees
- **[NEW] src/types/employee.ts:** Interface Employee
- **[NEW] src/lib/employee.ts:** Utility syncEmployeeName()
- **[NEW] src/pages/dashboard/admin/AdminEmployees.tsx:** Halaman CRUD + Excel import
- **[CHANGE] src/App.tsx:** Tambah route `/dashboard/admin/employees`
- **[CHANGE] src/pages/dashboard/DashboardLayout.tsx:** Tambah menu "Database Karyawan"
- **[CHANGE] src/pages/auth/AuthCallback.tsx:** Auto-sync nama dari employee master data
- **[CHANGE] src/pages/auth/Register.tsx:** Auto-sync nama dari employee master data
- **[CHANGE] src/pages/kiosk/Profile.tsx:** Sync nama + info "Nama akan disesuaikan"
- **[CHANGE] src/pages/portal/PortalProfile.tsx:** Sync nama saat edit profil
- **[CHANGE] package.json, Home.tsx, DashboardLayout.tsx, PortalLayout.tsx:** v5.2.3 → v5.3.0 (MINOR)

v5.2.4 — QRIS Otomatis Stock Fix & Order of Operations (PATCH)
----------------------------------------------------------------------------------
### Root Cause Analysis (RCA):
- **[FIX] QRIS Otomatis stock lost on failure:** Metadata update di `transactions.ts:504-513` bisa gagal silent (network glitch). Akibatnya `stock_deducted` = `undefined` di metadata. OLD guard `!tx?.metadata?.stock_deducted` di `restoreTransactionStock` menganggap undefined = skip → stock tidak pernah di-restore. Sudah diperbaiki di v5.2.3 dengan guard `=== false`.
- **[FIX] Wrong variable name in auto-cleanup:** `receiptImage` (undefined) vs `receiptUploaded` — filter receipt image tidak pernah berfungsi. Sekarang pake `receiptUploaded`.
- **[FIX] Order of operations:** `background-jobs.js` (auto-cleanup) dan `payments.ts` (iPaymu callback) update status ke "failed" SEBELUM `restoreTransactionStock`. Jika restore error (uncaught), status terkunci "failed" tanpa retry. Sekarang restore stock DULU, baru update status.

### Perubahan:
- **[CHANGE] `src/services/background-jobs.js`:** Swap order — `restoreStock(tx.id)` sebelum status update. Fix variable `receiptImage` → `receiptUploaded`.
- **[CHANGE] `src/routes/payments.ts`:** Failed flow dipisah dari shared status update — restore stock dulu, baru update status ke "failed". Paid flow tetap update status dulu (order existing).

### Versi:
- v5.2.3 → v5.2.4 (PATCH)
v5.6.1 — Conditional card form rendering and live branching fix (PATCH)
----------------------------------------------------------------------------------
- [FIX] Portal card forms now use the same condition engine as builder preview, including option value/label normalization and array answers.
- [FIX] Card navigation is clamped when conditional answers change the visible question count, preventing blank cards.
- [QA] `npm run lint` passes.
